apiVersion: 1

providers:
  - name: rabbitmq-notification
    orgId: 1
    folder: RabbitMQ Notification
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards
